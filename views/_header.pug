header.header
  .header__logo
    img(src="/img/PhLogo.svg", alt="Pharmacy logo")
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Logout
      |         
      a.nav__el(href='#')
        img.nav__user-img(src='/img/users/user-10.jpg', alt='User photo')
        |           
        span= user.name
    else
      a.nav__el(href="/login", target="_blank") Log in 
      a.nav__el.nav__el--cta(href='/signup', target="_blank") Signup